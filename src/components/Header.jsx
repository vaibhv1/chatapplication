import React from "react";
import './Header.css';

const Header = ({ selectedUser }) => {
  return (
    <header className="header">
      <h1 className="header-title">Chat Application</h1>
    </header>
  );
};

export default Header;